# Assets

| File | Status |
|---|---|
| `logo.svg` | Original mark (sync-arrows + lock), ready to use |
| `screenshots/dashboard.png` | Placeholder — captured once the CLI dashboard exists (Phase 9) |
| `screenshots/peer-pairing.png` | Placeholder — captured once peer authorization UX exists (Phase 4/6) |
| `banner.png` | Placeholder — a rendered/exported banner version of `logo.svg` for the README header |

Screenshots are added as each relevant phase lands — a screenshot of a
feature that doesn't exist yet would be misleading, so this directory only
holds real captures plus this tracking table.
