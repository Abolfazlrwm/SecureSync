"""SecureSync — filesystem infrastructure adapters.

Concrete implementations of the ``domain.watcher.FileWatcher`` port. See
``docs/architecture.md`` for how this package fits into the layering.
"""
