"""SecureSync — chunking infrastructure adapters.

Concrete implementations of the ``domain.chunking`` ports
(``ChunkingStrategy``, ``ChunkReader``, ``ChunkHasher``, ``ChunkWriter``,
``ChunkRepository``). See ``docs/architecture.md`` for how this package
fits into the layering.
"""
